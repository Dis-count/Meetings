首先对于n个人，存在我定义的 S_n...S_1

在（S_n，S_n-1）中 存在多少改变点就对应着 不满意联盟组合的值 有多少斜率变化的点


因此当使用的机器数量给定的时候，就会确定区间（S_n，S_n-1），

必须找到不等式交叉的点（S-补贴值）,或者通过不等式放缩的方法


筛选掉一系列冗余的不等式条件 由 S_n = c(s_n-1) - c(s_n)



%3. 应该可以从这个条件推出 active 的约束（最不满意联盟） 使用的一定都是一台机器
如何证明？？？？



问题：1.  模型的问题  为什么是 V 而不是 s  Answer :

这样使得我们从一个更宏观的角度来看待这个模型，矩阵的表示会更为明显

模型中 矩阵 表示的想法正确  即将 x_{kj} 按行拉直 或 按列拉直 即可 （试着用小例子表示一下）

\checkmark

2. c(s) 定义

改为 c(s,m(s,P)) 即 机器数量是 关于集合 以及 定价的函数


注意讨论


4.同时对于去掉最后一个限制条件  产生的负值部分只存在S_1之后

After the discussion

注意 supermodular 的性质

1） 单机器调度是 supermodular 的典型特例，同时意味着是 NP-hard 类型的，所以如何说明加入了定价模型之后就变得在多项式内可解了？？

2） 讨论 定价 对 supermodular 性质强弱的影响。

注意找P的临界情况  P=0时  我们知道 game是 supermodular

随着 P 不断增大 supermodular 的性质会不断变弱，直至到 P 的上限。

至于超过上限之后，supermodular 的性质如何变化 是变为 sub 还是别的 还不得而知。
